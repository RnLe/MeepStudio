"use client";

// Re-export from the new location for backward compatibility
export { useEditorStateStore } from '../stores/editor';
export type { Tab, TabType, MainTabType, SubTabType } from '../stores/editor/types';